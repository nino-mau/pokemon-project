meta {
  name: Get All Gyms
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/gyms
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Returns array of gyms", function() {
    const data = res.getBody();
    expect(data).to.be.an("array");
  });
}
