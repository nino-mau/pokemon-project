meta {
  name: Get All Trainers
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/trainers
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Returns array of trainers", function() {
    const data = res.getBody();
    expect(data).to.be.an("array");
  });
}
