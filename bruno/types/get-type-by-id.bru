meta {
  name: Get Type By ID
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/api/types/{{typeId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Returns type object", function() {
    const data = res.getBody();
    expect(data).to.be.an("object");
    expect(data).to.have.property("_id");
    expect(data).to.have.property("name");
  });
}
