meta {
  name: Get All Types
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/types
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Returns array of types", function() {
    const data = res.getBody();
    expect(data).to.be.an("array");
  });
}
