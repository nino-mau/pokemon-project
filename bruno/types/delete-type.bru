meta {
  name: Delete Type
  type: http
  seq: 5
}

delete {
  url: http://localhost:3000/api/types/{{typeId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Deletes type successfully", function() {
    const data = res.getBody();
    expect(data).to.be.an("object");
    expect(data.message).to.equal("Type deleted successfully");
  });
}
