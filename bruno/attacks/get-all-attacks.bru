meta {
  name: Get All Attacks
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/attacks
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Returns array of attacks", function() {
    const data = res.getBody();
    expect(data).to.be.an("array");
  });
}
